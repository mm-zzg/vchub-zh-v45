# 导出模型

### 导出步骤和格式

1.  步骤为：文件—导出—导出选定对象。
2.  格式为 GLB 文件（VC Hub 环境只支持 GLB 格式文件导入）。

    ![alt text](44.png)

### 导出名称和大小规范

1.  名称规范为：名称只能包含英文字母、中文、数字和下划线，且只能以英文子母和中文开头。
2.  导出模型单次大小不能超过 50M，VC Hub 环境只能支持小于 50M 的模型文件导入。

###  最终导出选项设置

![alt text](45.png)

